# Runtime dependencies: pyproject.toml [project.dependencies] (single source of truth).
# GitHub Actions workflows: pip install -r requirements.txt
# CI / local dev (tests, ruff, mypy): pip install -e ".[dev]"
# Do not pin packages here — edit pyproject.toml only.
-e .
